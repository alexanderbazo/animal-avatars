{
  "name": "animal-avatars.js",
  "version": "0.0.1",
  "description": "A client side javascript library to generate random avatars",
  "main": "index.js",
  "watch": {
    "build": {
      "patterns": [
        "lib/**/*"
      ],
      "extensions": "js,css,html"
    }
  },
  "scripts": {
    "build": "node build.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "npm-watch"
  },
  "author": "Alexander Bazo <alexanderbazo@googlemail.com>",
  "license": "MIT",
  "devDependencies": {
    "npm-watch": "^0.6.0"
  }
}
